SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=

# Comma-separated Slack user IDs allowed to run privileged commands such as
# /remote-control (which starts a full host-control session). Empty means no one
# is authorized. Bot messages are never treated as operators.
REMOTE_CONTROL_OPERATORS=
