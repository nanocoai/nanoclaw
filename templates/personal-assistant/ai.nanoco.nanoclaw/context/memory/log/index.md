# Log — the time spine

One global chronological record. Monthly files (`YYYY-MM.md`), dated entries, **bold
entity names**, source refs. Append a line for whatever happens: a meeting processed, a
deal moving, a decision, a structural memory change. Per-entity views come from grepping
this — never from copies. Create the current month's file on first write.
