---
schedule: "0 11,14,17,20 * * *"
---
Inbox sweep — triage and HANDLE new messages and tasks. You are your principal's chief
of staff: the goal is that nothing addressed to them goes unhandled, not just that
memory is tidy. Check every connected source for new material since the last sweep
(read the tail of your task log for the last covered-through timestamp; first run:
cover the last 6 hours). For each new message/thread/meeting, decide: (1) HANDLE —
things you can do yourself: answer from memory/sources, prepare a draft for approval,
look something up, complete or schedule a follow-up. Do them now. (2) NEEDS YOUR
PRINCIPAL — judgment or action only they can take: add to memory/attention.md with
evidence refs and a proposed next action or a ready draft. (3) NOISE — skip. DM your
principal only when something is urgent/blocking OR you have drafts and proposals
ready for their decision — batch everything into ONE message; if nothing needs them,
send nothing. Afterward, memory upkeep per the doctrine: notable events into
memory/log/, dated facts appended to the relevant files (never rewrite a Core summary
from a delta), resolved attention items closed with evidence. Finish by appending one
task-log line: covered-through timestamp + counts handled/queued/skipped. (If
recurring source pulls get configured for this deployment, this task can move to a
higher frequency with a pre-task gate script that only wakes you when new material
exists — ask your principal's operator.)
