---
schedule: "0 17 * * 5"
---
Weekly memory maintenance pass. Review memory/ per the conventions in
memory/system/definition.md: merge duplicate or overlapping concept files, fix broken
or missing links (including links into sources/), update per-folder index.md files and
the root Map, slim any file approaching the size budget by splitting detail out, and
verify memory/attention.md items are still live (drop resolved ones, citing where they
resolved). Refresh memory/research-queue.md: check whether new material moved any
entity's data-current-through date, and mark stale-active entities needing an update
pass. Re-propose memory/priorities.md if reality has drifted from the ranked list
(marked PROPOSED until your principal ratifies). If memory/ is a git repository,
commit at the end (message: gardening: <date>). Report what changed in the DM: files
merged/created/updated, links fixed, attention items closed, queue rows touched.
