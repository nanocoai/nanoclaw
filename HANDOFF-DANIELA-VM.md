# פרומפט מסירה — המשך עבודה על פיילוט Shellano (שרת daniela)

הדבק את כל המסמך הזה כהודעה ראשונה בסשן Claude על שרת daniela.

---

אתה ממשיך עבודה על פיילוט Shellano. הסשן הקודם רץ על שרת eliabc (של אליה),
פיתח ובדק שם את הקוד, ופרס אותו לשרת הזה ב-2026-07-03. הכול כבר חי כאן.
המשתמש (אליה) אינו מתכנת — דבר איתו עברית פשוטה, בלי ז'רגון.

## מה נפרס לכאן היום (commit 729e5c6 ב-~/nanoclaw-v2)

**אקטיבציה v2 לפיילוט** — מחליפה את זרימת קודי ה-4 ספרות:

- `POST /provision` (פורט 3000, עם Bearer של HOST_PROVISION_TOKEN מה-.env)
  כבר לא יוצר סוכן מראש. הוא מנפיק קוד חד-פעמי בן 20 תווים (תוקף 24 שעות,
  טבלת `pilot_activations` ב-data/v2.db) ומחזיר
  `{"deepLink":"https://t.me/Nanoco_pilot_bot?start=<code>"}`.
- הסוכן נוצר רק כשמישהו לוחץ START בטלגרם, ונקשר לזהות הטלגרם של הלוחץ.
  טלפון/מייל מהטופס = מטא-דאטה בלבד, אף פעם לא מפתח ניתוב.
- קוד נשרף אחרי שימוש; פג/שרוף/לא מוכר → הודעה ידידותית בשפת ההרשמה.
- סוכן פעיל אחד לכל משתמש טלגרם — הפעלה חוזרת מנתבת לסוכן הקיים.
- חלון פיילוט 10 ימים נשמר ומוזרק להנחיות הסוכן (הוא יודע מתי הוא פג).
- שם קבוע לכל סוכנת פיילוט: **ג'ני** (הוחלף מ"ג'וני"), בלי הזמנת שינוי שם.
- כל המערכת הקיימת נשמרה: מודל Haiku נעול, תקרת עלות יומית 1$,
  חיווט פיקוח לדניאלה, מדידת שימוש, דשבורד. המסלול הישן (4 ספרות) עדיין
  עובד במקביל כגיבוי.

קבצים מרכזיים: `src/modules/pilot-activation/` (db, activation + טסטים),
`src/provision-handler.ts` (handleProvision מחודש + provisionPilotAtPress),
`src/channels/telegram-pilot.ts` (interceptor חדש לפני המסלול הישן),
`src/db/migrations/020-pilot-activations.ts`.

**מה כבר אומת כאן:** בנייה נקייה, 16/16 טסטים עוברים, המיגרציה הוחלה,
השירות עלה נקי, ו-curl ל-/provision החזיר לינק תקין לבוט
@Nanoco_pilot_bot עם קוד 20 תווים (הקוד SWQHWBQPLU4DR3RYMPVZ נשאר pending —
אפשר להשתמש בו לבדיקה חיה או להנפיק חדש).

**ביטול אם צריך:** `cd ~/nanoclaw-v2 && git revert HEAD --no-edit && pnpm run build && systemctl --user restart nanoclaw-v2-207e7c29`

הערה: תיקון "ההודעה המשולשת" (idempotent-outbound) כבר היה קיים בקוד המקומי
כאן (commit 125cfa6 של הסשן המקומי הקודם) ופעיל — לא נגענו בו.

## המשימות שלך, לפי סדר

### 1. חיבור אנתרופיק חדש + תיקון שגיאות 401 (דחוף — הסוכנים כאן לא עונים!)

רקע: יש 617 שגיאות "401 Invalid authentication credentials" ב-
logs/nanoclaw.error.log. המפתח הקודם (של דניאל, מפתח חברה) **הופסק על ידי
החברה** — אין טעם לרענן אותו. ההחלטה של אליה: המערכת תרוץ על חשבון
האנתרופיק שלו (שעדיין פעיל). לכן:

**א. חבר את חשבון האנתרופיק של אליה על המכונה הזאת** (התחברות מקומית —
לא מעתיקים שום דבר משרת אחר):
```bash
claude setup-token   # או: claude ואז תהליך login
# זה ידפיס לינק — תן אותו לאליה, הוא לוחץ ומאשר בדפדפן עם החשבון שלו.
```

**ב. עדכן את הכספת המקומית:**
```bash
onecli secrets list                 # מצא את הסיקרט של Anthropic
# קח את הטוקן מ-~/.claude/.credentials.json (שדה accessToken):
onecli secrets update --id <secret-id> --value '<הטוקן>'
# אין צורך בהפעלה מחדש — ה-gateway קורא סיקרטים בכל בקשה.
```

**ג. פתרון קבע לתפוגת ה-8 שעות** — טוקן מהסוג הזה פג כל ~8 שעות. הקם
רענון אוטומטי מקומי (cron למשתמש exedev), למשל סקריפט שרץ כל 4 שעות:
מריץ `claude -p 'ok'` (מרענן את הטוקן דרך ה-refresh token), שולף את
accessToken מ-~/.claude/.credentials.json ומעדכן את הסיקרט ב-onecli.
בדוק שהסקריפט עובד פעם אחת ידנית לפני שסומכים עליו.

**ד. אימות:** שלח לדניאלה הודעה בטלגרם וודא שהיא עונה; ודא שהשגיאות
בלוג הפסיקו.

**חשוב לומר לאליה בסיום:** כל הפיילוט רץ עכשיו על המכסה האישית של החשבון
שלו. לבדיקות זה בסדר; לפני משתמשים אמיתיים — מפתח API ייעודי
(console.anthropic.com, תשלום לפי שימוש) ולהחליף בכספת.

### 2. בדיקה חיה של האקטיבציה עם אליה

בקש מאליה ללחוץ: https://t.me/Nanoco_pilot_bot?start=SWQHWBQPLU4DR3RYMPVZ
מצופה: ברכת "אהלן … אני ג'ני" בעברית (מגיעה גם בלי תיקון ה-401 — היא סטטית),
ואז תשובות אמיתיות של הסוכנת (דורש שה-401 תוקן).
לחיצה שנייה על אותו לינק → "הקוד הזה כבר נוצל".
הרשמה חוזרת + לחיצה → "הסוכנת שלך ג'ני כבר פעילה כאן".

### 3. חיבור הטופס באתר

הטופס ב-shellano.com (ובגרסת הפיתוח nanohubdemo.vercel.app) צריך לקרוא
ל-POST /provision של השרת הזה עם ה-Bearer. בדוק איך האתר קרא לכאן עד היום
(ה-handler הישן כבר קיבל קריאות מ-nanohubdemo) וודא שהמסלול עדיין תקין
מקצה לקצה. גוף הבקשה: {name, phone, email, gender, lang, ts}.

### 4. סדר בנתונים ישנים (לא דחוף)

סוכני פיילוט ישנים שנוצרו מראש בזרימה הישנה ולא הופעלו מעולם — מועמדים
לניקוי (יש pilot-orphan-sweep). למשתמשי פיילוט קיימים אין רשומת
pilot_activations — אם תרצה שגם עליהם יחול "סוכן אחד למשתמש", צריך backfill.
שקול עם אליה.

## אזהרות

- לא לגעת ב-ONECLI_BIND_HOST ובשרשרת ה-OneCLI. מופע gateway יחיד מנוהל.
- הפעלות שירות רק דרך systemctl --user (לא npm run start ידני).
- הבוט האישי של דניאלה (TELEGRAM_BOT_TOKEN הראשון ב-.env) לא קשור — לא לגעת.
- יש pre-commit hook שמריץ prettier — commit ראשון עלול "לגעת" בקבצים רבים; זה תקין.
- העבודה מול אליה: שלב-שלב, לדווח, לא להתקדם בלי אישור שלו.
